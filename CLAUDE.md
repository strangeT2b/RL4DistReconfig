# RL4DistReconfig Claude Entry Point

Read `./agent.md` before making code, experiment, evaluation, or documentation changes. Treat `agent.md` as the canonical root project guide.

For CoT distillation work, also read:

```text
CoT_distill/agent.md
```

Keep this file short. Put durable project-wide guidance in `agent.md`, CoT-specific guidance in `CoT_distill/agent.md`, and dated historical notes in `docs/archive/agent-notes/`.
